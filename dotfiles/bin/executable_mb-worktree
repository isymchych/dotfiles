#!/usr/bin/env bash
# zsh-completion: completions/_mb-worktree

set -euo pipefail

: "${ACCEL_OS:?ACCEL_OS must point at repo root}"
export AI_CWD="$PWD"
exec node "$ACCEL_OS/scripts/scripts/worktree.ts" "$@"
