#!/usr/bin/env bash
# zsh-completion: completions/_mb-audio

set -euo pipefail

: "${ACCEL_OS:?ACCEL_OS must point at repo root}"
exec "$HOME/bin/accel-node" "$ACCEL_OS/scripts/scripts/mb-audio.ts" "$@"
